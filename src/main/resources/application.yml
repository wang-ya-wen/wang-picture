server:
  port: 8123
  servlet:
    context-path: /api
spring:
    application:
      name: wang-picture
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/wang-picture?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
      password: Wyw202007@
      username: root
    servlet:
       multipart:
         max-file-size: 2MB
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
      db-config:
        logic-delete-field: isDelete
        logic-delete-value: true
        logic-not-delete-value: false
#接口文档配置
knife4j:
  enable: true
  openapi:
    title: 接口文档
    version: 1.0
    group:
      test1:
        group-name: 默认分组
        api-rule: package
        api-rule-resources:
          - com.wang.wangpicture.controller
